# Gold Price YouTube Bot — Dependencies

# ── Web scraping ──────────────────────────────────────────────────────────────
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# ── AI script generation ──────────────────────────────────────────────────────
anthropic>=0.50.0

# ── Thumbnail generation ──────────────────────────────────────────────────────
Pillow>=10.2.0

# ── TTS: ElevenLabs — most natural voices, set ELEVENLABS_API_KEY in .env ────
elevenlabs>=1.9.0

# ── TTS: edge-tts — free Microsoft Neural voices, no API key (FALLBACK) ──────
edge-tts>=6.1.0

# ── TTS: Google Cloud TTS — set USE_CLOUD_TTS=true in .env to enable ─────────
# google-cloud-texttospeech>=2.16.0

# ── Thumbnail: Playwright — correct Indic script rendering ────────────────────
# playwright>=1.40.0  (install separately: pip install playwright && playwright install chromium)

# ── Video creation ────────────────────────────────────────────────────────────
moviepy>=2.0.0
numpy>=1.26.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9     # bundles ffmpeg binary — no separate ffmpeg install needed

# ── YouTube upload ────────────────────────────────────────────────────────────
google-api-python-client>=2.100.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# ── Environment variables ─────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── HeyGen AI avatar (future use — avatar_video.py) ──────────────────────────
# No extra packages needed; uses requests (already above)
